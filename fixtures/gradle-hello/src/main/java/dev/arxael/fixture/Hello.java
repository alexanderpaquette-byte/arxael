package dev.arxael.fixture;

/** Minimal compilable unit so the smoke build exercises a real gradle compile task. */
public final class Hello {
    public static String greeting() {
        return "hello from arxael fixture";
    }
}
